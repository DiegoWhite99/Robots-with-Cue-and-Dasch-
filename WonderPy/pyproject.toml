[project]
name = "wonderpy"
version = "0.3.0"
description = "Python API for working with Wonder Workshop robots"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bleak>=2.1.1,<4",
    "mock>=5.2,<6",
    "svgpathtools>=1.7,<2",
]
